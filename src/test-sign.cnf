#
# OpenSSL example configuration file.
# This is mostly being used for generation of certificate requests.
#

# This definition stops the following lines choking if HOME isn't
# defined.
HOME			= .
RANDFILE		= $ENV::HOME/.rnd

# Extra OBJECT IDENTIFIER info:
#oid_file		= AROoid
oid_section		= ARRL_extensions

# To use this configuration file with the "-extfile" option of the
# "openssl x509" utility, name here the section containing the
# X.509v3 extensions to use:
extensions		= usr_cert
# (Alternatively, use a configuration file that has only
# X.509v3 extensions in its main [= default] section.)

[ ARRL_extensions ]
# Note: ARRL is registered with IANA
ARRLrootOID		= 1.3.6.1.4.1.12348
LOTWbaseOID		= ${ARRLrootOID}.1
AROcallsign		= ${LOTWbaseOID}.1
QSONotBeforeDate	= ${LOTWbaseOID}.2
QSONotAfterDate		= ${LOTWbaseOID}.3

[ v3_ARRL ]

# Extensions to add to a certificate request

basicConstraints = CA:FALSE
keyUsage = nonRepudiation, digitalSignature, keyEncipherment
1.3.6.1.4.1.12348.1.2 = DER:$ENV::qnotbefore
1.3.6.1.4.1.12348.1.3 = DER:$ENV::qnotafter
1.3.6.1.4.1.12348.1.4 = DER:$ENV::dxcc
